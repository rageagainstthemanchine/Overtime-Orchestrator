###############################
# Simplified Overtime Script  #
###############################

# ---- Date Range ----
SINCE=2025-01-01
UNTIL=2025-07-31

# ---- Git Commits ----
# Comma-separated author emails (case-insensitive) to include
MY_EMAILS=email1@example.com,email2@example.com

# Root folder where repositories live (recursively scans for .git)
REPOS_ROOT=/home/arch/company/repositories

# ---- Bitbucket (optional) ----
USE_BITBUCKET=false
BITBUCKET_USER=
BITBUCKET_APP_PASSWORD=
BITBUCKET_WORKSPACE=
# Comma-separated repo slugs
BITBUCKET_REPO_SLUGS=repo1,repo2

# ---- Calendar (ICS only) ----
GOOGLE_CALENDAR_ICS=/home/arch/calendars/export.ics
# Titles to exclude (case-insensitive). Typically OOO / PTO events.
EXCLUDED_CALENDAR_TITLES=Out of office,PTO,OOO
# Personal PTO dates (YYYY-MM-DD) treated as holidays for work window suppression
PTO_DAYS_STR=2025-07-31,2025-08-01

# ---- Slack (Search Mode Only) ----
USE_SLACK=true
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# User IDs whose authored messages count as activity evidence
SLACK_USER_IDS=U12345678,U23456789
SLACK_CACHE_ENABLED=true
SLACK_FORCE_REFRESH=false
SLACK_CACHE_DIR=.slack_cache

# ---- Timezone / Holidays ----
LOCAL_TZ=America/New_York
HOLIDAYS_COUNTRY=US
HOLIDAYS_PROV=NY

# ---- Work Hours (new configurable intervals) ----
# Default work hours applied to all weekdays unless overridden below (comma-separated intervals HH:MM-HH:MM)
WORK_HOURS=09:00-18:00
# Optional per-day overrides (example with split lunch):
# WORK_HOURS_MON=09:00-12:00,13:00-17:30
# WORK_HOURS_TUE=
# WORK_HOURS_WED=
# WORK_HOURS_THU=
# WORK_HOURS_FRI=
# WORK_HOURS_SAT=10:00-14:00
# WORK_HOURS_SUN=
# Weekend suppression (true = weekends count entirely as extra, ignore any weekend overrides)
WEEKENDS_COUNT_AS_EXTRA=true
